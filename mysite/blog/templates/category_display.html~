{% extends 'base.html' %}
{% load custom_filters %}

{% block content %}
	{% if posts %}
		{% for post in posts %}
		<div class="box">
			<div>
				<div id="posting_date">Date : {{ post.post_date }}</div>
				<div></div>
			</div>
			<div id="post_image">
				<div></div>
				<img src="{{ post.image }}" alt="Post Image" >
			</div>
			<div id="post_title"><a href="/post_{{ post.id }}/">{{ post.title }}</a></div>
			{% autoescape off %}
			<div id="posting">{{ post.article|format_post|truncate }}</div>
			{% endautoescape %}
			<div id="post_footer">
				<a href="/post_{{ post.id }}/">Read more.</a>
			</div>
		</div>
		{% endfor %}
	{% else %}
		<div>
			<div id="post_infra">
				<div id="post_title">{{ message }}</div>
			</div>		
		</div>
	{% endif %}
	
{% endblock %}